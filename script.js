// script.js

// Exemplo de validação de formulário (a ser implementado na seção de contato)
const form = document.getElementById('contactForm');
form.addEventListener('submit', (event) => {
    event.preventDefault();
    // Aqui você pode adicionar a lógica para validar os dados do formulário e enviar um email ou armazenar os dados
    alert('Formulário enviado com sucesso!');
});